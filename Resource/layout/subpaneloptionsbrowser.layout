"resource/layout/subpaneloptionsbrowser.layout"
{
  styles {
  }

  layout {
    place { controls=DescriptionLabel width=max }
    place { controls=OverlayHomePageLabel start=DescriptionLabel dir=down y=30 }
    place { controls=OverlayHomePage start=OverlayHomePageLabel width=280 dir=down y=5 }

    place { control=ClientBrowserAuthHomePage start=OverlayHomePage width=max dir=down y=25 }

    place { controls=ClearWebCacheButton,ClearAllCookiesButton start=ClientBrowserAuthHomePage dir=down y=30 spacing=5 }

    place { controls=Divider1 height=0 width=0 }
  }
}
